name: AI Implement

on:
  issues:
    types: [opened]

permissions: {}

concurrency:
  group: ai-issue-${{ github.event.issue.number }}
  cancel-in-progress: false

jobs:
  implement:
    name: Plan, implement and open PR
    permissions:
      contents: read # checkout and repository context
      issues: write # publish the plan and PR link
      pull-requests: read # let Claude inspect linked PR metadata
    if: >-
      github.event.issue.user.login == github.repository_owner &&
      startsWith(github.event.issue.title, '[AI]:')
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Check out main
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
        with:
          ref: main
          fetch-depth: 0
          persist-credentials: false

      - name: Plan with Claude
        id: plan
        uses: anthropics/claude-code-action@be7b93b1907a4abad570368f3c74b6fe3807510b # v1
        with:
          anthropic_api_key: ${{ secrets.ANTHROPIC_API_KEY }}
          prompt: |
            Actúa exclusivamente como arquitecto y planificador para el Issue
            #${{ github.event.issue.number }} de ${{ github.repository }}.

            TÍTULO:
            ${{ github.event.issue.title }}

            SOLICITUD:
            ${{ github.event.issue.body }}

            Lee CLAUDE.md y AGENTS.md. Investiga el HTML, CSS, JavaScript y
            controles de seguridad relacionados sin modificar archivos. Devuelve
            un plan ejecutable por Codex, con alcance, exclusiones, archivos,
            pasos, criterios verificables, validaciones técnicas y visuales,
            accesibilidad y riesgos. Marca blocked=true si falta una decisión
            humana, la petición es insegura o no puede planificarse con evidencia.
          claude_args: >-
            --max-turns 10
            --allowedTools "Read,Grep,Glob"
            --json-schema '{"type":"object","properties":{"blocked":{"type":"boolean"},"blocker":{"type":"string"},"plan":{"type":"string"}},"required":["blocked","blocker","plan"],"additionalProperties":false}'

      - name: Publish Claude plan
        env:
          GH_TOKEN: ${{ github.token }}
          ISSUE_NUMBER: ${{ github.event.issue.number }}
          PLAN_JSON: ${{ steps.plan.outputs.structured_output }}
        run: |
          plan=$(jq -r '.plan' <<<"$PLAN_JSON")
          blocked=$(jq -r '.blocked' <<<"$PLAN_JSON")
          blocker=$(jq -r '.blocker' <<<"$PLAN_JSON")
          {
            echo "## Plan automático de Claude"
            echo
            echo "$plan"
            if [ "$blocked" = "true" ]; then
              echo
              echo "### Bloqueo"
              echo "$blocker"
            fi
          } > /tmp/claude-plan.md
          gh issue comment "$ISSUE_NUMBER" --repo "$GITHUB_REPOSITORY" --body-file /tmp/claude-plan.md

      - name: Stop on planning blocker
        if: fromJSON(steps.plan.outputs.structured_output).blocked == true
        env:
          BLOCKER: ${{ fromJSON(steps.plan.outputs.structured_output).blocker }}
        run: |
          echo "Claude bloqueó la implementación: $BLOCKER" >&2
          exit 1

      - name: Implement with Codex
        id: codex
        uses: openai/codex-action@52fe01ec70a42f454c9d2ebd47598f9fd6893d56 # v1
        with:
          openai-api-key: ${{ secrets.OPENAI_API_KEY }}
          permission-profile: ":workspace"
          safety-strategy: drop-sudo
          effort: medium
          prompt: |
            Implementa el Issue #${{ github.event.issue.number }} en
            ${{ github.repository }} siguiendo AGENTS.md y CLAUDE.md.

            SOLICITUD ORIGINAL:
            ${{ github.event.issue.body }}

            PLAN VERIFICADO POR CLAUDE:
            ${{ fromJSON(steps.plan.outputs.structured_output).plan }}

            Conserva HTML, CSS y JavaScript nativos, identidad visual,
            accesibilidad, responsive y controles de seguridad. Modifica solo los
            archivos necesarios. No cambies workflows, instrucciones de agentes,
            credenciales ni la propia automatización. No hagas push ni merge.

      - name: Enforce automation boundaries
        run: |
          test -n "$(git status --short)" || { echo "Codex no produjo cambios" >&2; exit 1; }
          changed=$( { git diff --name-only; git ls-files --others --exclude-standard; } | sort -u )
          protected=$(printf '%s\n' "$changed" | grep -E \
            '^(\.github/(workflows|actions)/|\.gitmodules$|\.gitattributes$|AGENTS\.md$|CLAUDE\.md$)' || true)
          test -z "$protected" || { echo "Codex modificó archivos protegidos:" >&2; echo "$protected" >&2; exit 1; }
          if printf '%s\n' "$changed" | grep -Eq '(^|/)\.env($|\.local$|\.production$|\.development$)'; then
            echo "Codex intentó confirmar un archivo de entorno real" >&2
            exit 1
          fi
          git diff --check

      - name: Validate implementation
        run: python3 .github/scripts/check_page_security.py

      - name: Create scoped GitHub App token
        id: app-token
        uses: actions/create-github-app-token@bcd2ba49218906704ab6c1aa796996da409d3eb1 # v3.2.0
        with:
          client-id: ${{ vars.AI_APP_CLIENT_ID }}
          private-key: ${{ secrets.AI_APP_PRIVATE_KEY }}
          permission-contents: write
          permission-issues: write
          permission-pull-requests: write

      - name: Create implementation branch
        id: publish
        env:
          APP_TOKEN: ${{ steps.app-token.outputs.token }}
          ISSUE_NUMBER: ${{ github.event.issue.number }}
        run: |
          test -n "$APP_TOKEN" || { echo "La GitHub App no entregó un token temporal" >&2; exit 1; }
          branch="ai/issue-${ISSUE_NUMBER}-${GITHUB_RUN_ID}"
          git config user.name "Codex Automation"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git config core.hooksPath /dev/null
          git switch -c "$branch"
          git add --all
          git commit -m "ai: implement issue #${ISSUE_NUMBER}"
          git remote set-url origin "https://x-access-token:${APP_TOKEN}@github.com/${GITHUB_REPOSITORY}.git"
          git push origin "HEAD:refs/heads/$branch"
          echo "branch=$branch" >> "$GITHUB_OUTPUT"

      - name: Open pull request
        env:
          GH_TOKEN: ${{ steps.app-token.outputs.token }}
          ISSUE_NUMBER: ${{ github.event.issue.number }}
          BRANCH: ${{ steps.publish.outputs.branch }}
        run: |
          cat > /tmp/pr-body.md <<EOF
          Closes #${ISSUE_NUMBER}

          Implementación generada por Codex a partir del plan automático de Claude.

          El merge se realizará únicamente cuando CI finalice correctamente y la
          revisión automática de Claude apruebe el cambio.
          EOF
          pr_url=$(gh pr create --repo "$GITHUB_REPOSITORY" --base main --head "$BRANCH" \
            --title "AI: implementa #${ISSUE_NUMBER}" --body-file /tmp/pr-body.md)
          gh issue comment "$ISSUE_NUMBER" --repo "$GITHUB_REPOSITORY" \
            --body "Codex completó la implementación: $pr_url"
